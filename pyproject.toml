[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "alphaforge-core"
version = "0.1.0"
description = "Investment Intelligence Operating System"
requires-python = ">=3.11"